<Window x:Class="MikroTikTestingApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MikroTikTestingApp"
        xmlns:viewmodels="clr-namespace:MikroTikTestingApp.ViewModels"
        xmlns:views="clr-namespace:MikroTikTestingApp.Views"
        mc:Ignorable="d"
        Title="MT-pablo" Height="450" Width="800" Background="#FF1D916C" Loaded="WindowIsLoaded" WindowStyle="ToolWindow" ResizeMode="CanMinimize" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <DataTemplate x:Name="CameraViewTemplate" DataType="{x:Type viewmodels:SettingsViewModel}">
            <views:SettingsView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate x:Name="StatusViewTamplate" DataType="{x:Type viewmodels:StatusViewModel}">
            <views:StatusView DataContext="{Binding}"/>
        </DataTemplate>
        <DataTemplate x:Name="MainViewTemplate" DataType="{x:Type viewmodels:MainViewModel}">
            <views:MainView DataContext="{Binding}"/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1.5*"/>
            <ColumnDefinition Width="6*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="10*"/>

        </Grid.RowDefinitions>
        <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal">
            <TextBlock x:Name="menuText" Text="Hello elo" FontSize="30" Width="auto"/>
            <TextBlock x:Name="menuText2" Text="Hello elo" FontSize="30" Width="auto"/>
        </StackPanel>
        <!-- action buttons -->
        <StackPanel Margin="5,0,5,0" Grid.Row="1">
            <Button x:Name="MainView" Content="main" FontSize="20" Focusable="False" Height="50" Click="MainView_Click"/>
            <Button x:Name="CameraView" Content="cam" FontSize="20" Focusable="False" Height="50" Click="CameraView_Click"/>
            <Button x:Name="StatusView" Content="Status" FontSize="20" Focusable="False" Height="50" Click="StatusView_Click"/>
        </StackPanel>
        <ContentControl Grid.Column="1" Grid.Row="1" Grid.RowSpan="1" Content="{Binding}" Margin="0,0,0,0"/>
    </Grid>
</Window>
